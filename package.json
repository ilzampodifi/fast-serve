{
  "name": "fast-serve",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest",
    "@types/compression": "^1.8.1",
    "@types/express": "^5.0.3"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "compression": "^1.8.1",
    "express": "^5.1.0",
    "mongoose": "^8.17.1",
    "zod": "^4.0.17"
  },
  "scripts": {
    "dev": "bun run --watch server.ts",
    "build": "rm -rf dist && bun build server.ts --outdir dist --target bun --sourcemap",
    "db:seed": "bun run src/db/seeder.ts"
  }
}
