{
  "name": "fast-serve",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "express": "^5.1.0"
  },
  "scripts": {
    "dev": "bun run --watch server.ts",
    "build": "rm -rf dist && bun build server.ts --outdir dist --target bun --sourcemap"
  }
}
